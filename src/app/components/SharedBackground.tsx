'use client';

/**
 * SharedBackground — the grain overlay is handled globally via body::before in globals.css.
 * The Seed of Life geometry and radial glow live within each page's hero section.
 * This component is intentionally empty so the layout structure is preserved.
 */
const SharedBackground: React.FC = () => null;

export default SharedBackground;
